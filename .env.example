# Android emulator uses 10.0.2.2 by default. For a phone, use your computer's LAN IP.
EXPO_PUBLIC_MOCK_API_URL=http://192.168.1.10:3001

# Required only for Google Maps in an Android release build. Do not commit a real key.
GOOGLE_MAPS_API_KEY=replace_with_restricted_key
